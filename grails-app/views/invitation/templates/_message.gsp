فروشگاه اینترنتی زنبیل

${inviter}

${message}

${createLink(uri: '/', absolute: true)}?invitation=${identifier}

${createLink(controller: 'customer', action: 'register', absolute: true)}?invitation=${identifier}

